services:
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    networks:
      - app-network
    restart: always

networks:
  app-network:
    driver: bridge
